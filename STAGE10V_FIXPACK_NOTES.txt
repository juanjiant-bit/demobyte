Stage 10V Fixpack

Fixed critical compile blockers:
- Added missing AudioDecisionState struct to audio_engine.h
- Declared update_dominant_decision() in audio_engine.h
- Added missing ModulationBus fields:
  * bass_gate
  * bass_decay
  * stutter_amount
- Added missing DspChain header members/methods:
  * LpFilter lp_
  * hp_mix_
  * lp_mix_
  * set_dual_filter_control(float)

Fixed important integration issues:
- update_dominant_decision() now reads audio_decision_ events correctly
- update_modulation_bus() now uses the intended 3-zone Macro 2 semantics
- Removed a sensitive direct harmonic_blend_ dependency from NoteVoice FM shaping
- Applied note_layer_trim_ to the NoteVoice combined layer stack
- Added a working dual-filter control path in DspChain and LP processing in the DSP chain

Note:
- The analysis mentioned bass_hold_steps_/bass_force_advance_, but those symbols are not referenced
  in this Stage 10V codebase, so they were not required for compilation.
