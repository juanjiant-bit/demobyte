OPTIMIZATION AUDIT — PASS C

Focus:
- remove the remaining audio_engine.cpp runtime sinf() calls from the Core0 hot path
- keep the character of the engine intact while lowering ISR cost

Changes:
- added dsp/fast_trig.h with a small 256-step sine lookup table plus linear interpolation
- audio/audio_engine.cpp now uses fast_sine01() for:
  - cached slow snapshot drift
  - cached slow snapshot air
  - sub oscillator
- operators.cpp was intentionally left unchanged so PM/operator tone keeps its exact current character

Why this pass is coherent:
- it targets the remaining explicit trig call inside process_one_sample()
- it avoids large architectural changes
- it preserves the operator engine timbre while lightening the most obvious hot-path sine work

Risk:
- extremely low; only the sub and ultra-slow motion sources changed implementation
- any audible change should be subtle and mostly in phase micro-detail, not in musical behavior

Still pending:
- deeper DSP-chain cost audit
- MacroMotion cost reduction, if needed, without flattening its behavior
- optional block-rate caching for additional slow control sources
