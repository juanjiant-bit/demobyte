Stage 6C — Bug scrub pass

Fixed:
- SnapshotVoice was still using the older direct 4-layer blend in the final tree,
  while NoteVoice already had the newer morph + dry routing.
  This pass re-applies SnapshotVoice morph/dry routing so both voices are consistent.
- operators.cpp / op_tri() now handles negative wrapped phase safely.

Why this mattered:
- The project had started to drift between the intended design and the actual final cpp.
- SnapshotVoice was not responding to the newer floatbeat-dry / morph logic the way NoteVoice did.
- Fixing this should make scene behavior more coherent and patch response more predictable.
