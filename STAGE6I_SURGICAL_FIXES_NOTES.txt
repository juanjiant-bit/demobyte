Stage 6I — Surgical fixes

Applied exact structural fixes:
1) macro_density_ -> macro_motion_.density_s in audio_engine.cpp
2) SnapshotVoice final routing replaced with morph + dry path
3) SnapshotVoice float_mix made mutable before clamping
4) Removed dead operator_amount remnants from voice blocks
5) floatbeat_seed.h now uses float_ops.h instead of duplicated wrap/sine/tri/softclip helpers
6) op_pm signature reconciled and floorf removed from hot path
7) DspChain reset/active-mask sanity for STUTTER

This pass is meant to close the three structural issues identified in the review,
plus the small cleanup items tied directly to them.
