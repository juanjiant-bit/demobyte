Optimization audit pass F

Applied:
- GrainFreeze hot-path cleanup:
  * Added active_ cache so DspChain can skip it without recomputing freeze/amount logic.
  * Replaced per-sample crossfade division with Q15 accumulator (XFADE_STEP_Q15).
- Reverb:
  * Added active_ cache updated in set_wet().
  * process() now branches on cached active_ instead of comparing wet_q15_ every sample.
- Chorus:
  * Removed dead read-index work from process().
  * Reused cached phase index for both channels.

Why these changes:
- They target DSP-heavy modules without changing the instrument architecture.
- They reduce hot-path arithmetic and branch work in Core0/ISR.
- They preserve sound character more safely than redesigning the DSP topology.
