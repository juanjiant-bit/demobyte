Stage 6B — Consistency + calibration pass

What this pass does:
- Reconciles header state added in later stages with the actual macro logic in audio_engine.cpp.
- Reinstates the intended decoupled control behavior:
  * perf_macro_
  * low_depth_ctrl_
  * float_body_ctrl_
  * morph_ctrl_
  * layer_morph_macro_
  * floatbeat_dry_macro_
- Reapplies morph + dry routing to note_voice and snapshot_voice so the newer fields
  are not just present in headers but actually drive sound behavior.
- Slightly harmonizes sub mix with the decoupled control model.

Important:
- This is a structural consistency pass, not a full build-verified firmware release.
- It should reduce drift between "declared" behavior and "actual" behavior in the codebase.
