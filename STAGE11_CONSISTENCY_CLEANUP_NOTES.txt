Stage 11 — Consistency cleanup

What this pass did:
- removed obvious duplicated transport/state mirroring lines
- deduplicated helper declarations and transport accessor declarations
- normalized comments so they describe the current branch instead of old stage history
- cleaned stale stage-specific comment wording in AudioEngine
- clarified that io/input_router.cpp is the authoritative current control map
- improved semantic comments around:
  * Macro 2 zones
  * bass transport fallback
  * audio decision detection/stabilization
  * NoteVoice articulation
  * transport exposure via StateManager

Intent:
- reduce dead comments, duplicated glue, and semantic drift
- make the branch more coherent for proto testing and later maintenance
