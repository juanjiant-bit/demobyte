Stage 10A — Macro core / safe first implementation

Goal:
- begin implementing the new macro semantics in small, low-risk steps

Reused controls:
- P5 / PARAM_FILTER_MACRO -> breath_amount
- P6 / PARAM_MORPH        -> harmonic_blend

Added:
- EvalContext fields:
  * breath_amount
  * harmonic_blend
- AudioEngine smoothed states:
  * breath_amount_
  * harmonic_blend_
- ModulationBus with initial destinations:
  * note_fm_byte
  * note_fm_float
  * note_fold
  * note_decay
  * drum_decay
  * harmonic_support

Applied in this stage:
- NoteVoice now receives a gentle FM-like layer from bytebeat/floatbeat
  while keeping the touched note as the pitch carrier.
- A small harmonic support layer appears when harmonic_blend is low.
- Drum decay gets a very small breathing bias.

Intention:
- move toward the new instrument behavior without breaking the current hybrid engine
- keep this first pass conservative so the next step can focus on bug checking and listening
